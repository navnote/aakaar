{
	"name": "@aakaar/scripts",
	"version": "0.0.2",
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"registry": "tsx src/registry.mts",
		"check-deps": "tsx src/check-deps.ts",
		"ts": "tsc --noEmit",
		"clean": "rm -rf dist",
		"clean:nm": "rm -rf node_modules"
	}
}
